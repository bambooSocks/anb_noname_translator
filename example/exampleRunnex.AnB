
Sigma0:
    yes, no

Agents:
    X, server

Actors:
    a, b
    dishonest I

Knowledge:
    X: X, inv(pk(X))
    server: X

Actions:
    server: * y in {yes, no}
    server: if y = yes then
        server: new N1, N2
        server -> X: crypt(pk(X),pair(yes,N1),N2)
    else
        server: new N1
        server -> X: crypt(pk(X),no,N2)
    end

Bound: 4

# built in functions:
crypt, pk, inv, pair